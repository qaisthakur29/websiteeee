# Netlify config — Ruqfa Marketing (static site)
# Form data Google Apps Script se handle hota hai, no serverless functions needed

[build]
  publish = "frontend"

# SPA-style routing (optional, for single page)
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
